{% extends "base.html.twig" %}
{% block title %}Show destinatari{% endblock %}
    {% block page_content %}
        <!-- Page Content -->
        <div class="col-md-6 col-lg-6 col-sm-6 col-lg-offset-3 col-md-offset-3 col-sm-offset-3">
            <div class="box box-primary">
                <div class="box-header with-border">
                    <h3 class="box-header with-border">Destinatari</h3>
                    <a href="{{ path('destinatari_index') }}">Tornar a llistat destinataris</a><br>
                    <a href="{{ path('site_index') }}">Tornar a llistat llocs</a>
                </div>
                <div class="box-body">
                    <strong>
                        <i class="glyphicon glyphicon-user"></i>
                        Nom
                        <p class="text-muted">{{ destinatari.nom }}</p>
                        <hr>
                    </strong>
                    <strong>
                        <i class="glyphicon glyphicon-envelope"></i>
                        Email
                        <p class="text-muted">{{ destinatari.email }}</p>
                        <hr>
                    </strong>
                    {% if is_granted('ROLE_ADMIN') %}
                    <br>
                    <a href="{{ path('destinatari_edit', { 'id': destinatari.id }) }}"><span
                                class="glyphicon glyphicon-pencil"></span>Editar</a> <br>
                </div>
                <div class="box-footer">
                    <a href="{{ path('destinatari_delete', { 'id':destinatari.id }) }}" onclick="
return confirm('¿Estas segur que vols eliminar el destinatari?')">
                        <button class="btn btn-danger">Eliminar destinatari</button></a>

                    {% else %}
                </div>
                {% endif %}
            </div>
        </div>
        </div>
        <!-- /.container -->
    {% endblock %}
